def solve(s):
    a_string = s.split(' ')
    d=' '.join((word.capitalize() for word in a_string))
    return d

if __name__ == '__main__':
    fptr = open(os.environ['OUTPUT_PATH'], 'w')

    s = raw_input()

    result = solve(s)

    fptr.write(result + '\n')

    fptr.close(
